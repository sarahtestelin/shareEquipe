{% extends 'base.html.twig' %} {% block title %}{{ parent() }}Ajout de sous catégories{% endblock
%} {% block body %}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-10 text-primary">
            <h1 class="text-center text-primary mt-4 pt-4 display-1 fw-bold">Ajout de sous catégories</h1>
            {{form(form)}}
            {% for message in app.flashes('notice') %}
            <h2 class="alert alert-warning text-center mt-4 mb-4" role="alert">
                {{ message }}
            </h2>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}